{% extends 'new_site/account/compte.html.twig' %}
    {% form_theme form 'widget/fields-block.html.twig' %}

{% set active = active ?? 'informations' %}

{% block breadcrumbs %}
    <div class="page-header breadcrumb-wrap">
        <div class="container">
            <div class="breadcrumb">
                <a href="{{ path('new_site') }}" rel="nofollow">Accueil</a>
                <span></span> <a href="{{ path('dashboard') }}">Mon Compte</a>
                <span></span> Informations personnelles
            </div>
        </div>
    </div>
{% endblock %}

{% block contenu %}

     {{ form_start(form, {'attr': {'role':'form', 'class': 'form'}}) }}
      {{ include('_includes/ajax/response.html.twig') }}
        <div class="row">
            <div class="form-group col-md-4">
              {#   <label>Nom d'utilisateur <span class="required">*</span></label> #}
                 {{ form_row(form.username)}}
                {# <input required="" class="form-control square" name="name" type="text" value="{{ app.user.username }}"> #}
            </div>
            <div class="form-group col-md-4">
             {{ form_row(form.nom)}}
                {# <label>Nom <span class="required">*</span></label>
                <input required="" class="form-control square" name="dname" type="text" value="{{ app.user.nom }}"> #}
            </div>
            <div class="form-group col-md-4">
             {{ form_row(form.prenoms)}}
                {# <label>Nom <span class="required">*</span></label>
                <input required="" class="form-control square" name="dname" type="text" value="{{ app.user.nom }}"> #}
            </div>
            <div class="form-group col-md-12">
             {{ form_row(form.contact)}}
               {#  <label>Contact <span class="required">*</span></label>
                <input required="" class="form-control square" name="email" type="email" value="{{ app.user.email }}"> #}
            </div>
            <div class="form-group col-md-12">
             {{ form_row(form.residence)}}
               {#  <label>Contact <span class="required">*</span></label>
                <input required="" class="form-control square" name="email" type="email" value="{{ app.user.email }}"> #}
            </div>
            {# <div class="form-group col-md-12">
                <label>Current Password <span class="required">*</span></label>
                <input required="" class="form-control square" name="password" type="password">
            </div>
            <div class="form-group col-md-12">
                <label>New Password <span class="required">*</span></label>
                <input required="" class="form-control square" name="npassword" type="password">
            </div>
            <div class="form-group col-md-12">
                <label>Confirm Password <span class="required">*</span></label>
                <input required="" class="form-control square" name="cpassword" type="password">
            </div> #}
            {# <div class="col-lg-12 mt-3">
                <div class="card mb-3 mb-lg-0">
                    <div class="card-header">
                        <h5 class="mb-0">Adresse de livraison</h5>
                    </div>
                    <div class="card-body">
                       {{ form_row(form.residence)}}
                        <a href="#" class="btn-small">Modifier</a>
                    </div>
                </div>
            </div> #}

            <div class="col-md-12">
                <button type="submit" class="btn btn-fill-out btn-ajax" name="submit" value="Submit"><span class="spinner-border  spinner-ajax spinner-border-sm d-none" role="status" aria-hidden="true"></span>Enregistrer</button>
            </div>

        </div>
 {{ form_end(form) }}

{% endblock %}


{% block scripts %}
    <script>
       
    </script>
{% endblock %}